naftiko: 1.0.0-alpha2
info:
  label: Azure Event Grid REST API — Operations
  description: 'Azure Event Grid REST API — Operations. 1 operations. Lead operation: Azure Event Grid REST API List operations.
    Self-contained Naftiko capability covering one Microsoft Azure Event Grid business surface.'
  tags:
  - Microsoft Azure Event Grid
  - Operations
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    MICROSOFT_AZURE_EVENT_GRID_API_KEY: MICROSOFT_AZURE_EVENT_GRID_API_KEY
capability:
  consumes:
  - type: http
    namespace: microsoft-azure-event-grid-operations
    baseUri: https://management.azure.com
    description: Azure Event Grid REST API — Operations business capability. Self-contained, no shared references.
    resources:
    - name: providers-Microsoft.EventGrid-operations
      path: /providers/Microsoft.EventGrid/operations
      operations:
      - name: listoperations
        method: GET
        description: Azure Event Grid REST API List operations
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.MICROSOFT_AZURE_EVENT_GRID_API_KEY}}'
  exposes:
  - type: rest
    namespace: microsoft-azure-event-grid-operations-rest
    port: 8080
    description: REST adapter for Azure Event Grid REST API — Operations. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/providers/microsoft-eventgrid/operations
      name: providers-microsoft-eventgrid-operations
      description: REST surface for providers-Microsoft.EventGrid-operations.
      operations:
      - method: GET
        name: listoperations
        description: Azure Event Grid REST API List operations
        call: microsoft-azure-event-grid-operations.listoperations
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: microsoft-azure-event-grid-operations-mcp
    port: 9090
    transport: http
    description: MCP adapter for Azure Event Grid REST API — Operations. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: azure-event-grid-rest-api
      description: Azure Event Grid REST API List operations
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: microsoft-azure-event-grid-operations.listoperations
      outputParameters:
      - type: object
        mapping: $.
